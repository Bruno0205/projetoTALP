{
  "name": "talp-tests",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "cucumber-js --require-module ts-node/register --require support/**/*.ts --require step-definitions/**/*.ts features/**/*.feature"
  },
  "dependencies": {
    "@cucumber/cucumber": "^11.0.1",
    "supertest": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "@types/supertest": "^6.0.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.3"
  }
}
